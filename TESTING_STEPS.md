# 決賽 Prototype 測試步驟

最後更新：2026-08-21

本文件給評審、PM 與 QA 直接操作「從碳客變捷客」概念原型。原型不需要登入、不需要網路 API，也不會送出真實會員、票務、付款或區塊鏈資料。

## 測試入口

- 線上民眾投票展示頁：<https://from-carbon-metro-vote.pages.dev/>
- 線上互動 prototype：<https://from-carbon-metro-vote.pages.dev/demo/>
- 本地 server：在 repo 根目錄執行 `python3 -m http.server 4173 --bind 127.0.0.1`，展示頁開啟 <http://127.0.0.1:4173/>，互動 prototype 開啟 <http://127.0.0.1:4173/demo/>
- 離線包：`offline/from-carbon-to-metro-offline.zip`
- 建議瀏覽器：Chrome、Edge 或 Safari 的近代版本
- 狀態重置：重新整理頁面即可回到初始 demo state
- 直接開啟 B2B 營運視圖：在 demo URL 加上 `?role=b2b` 或 `#b2b`

注意：GitHub Pages production URL 目前仍可能顯示舊版；線上投票送件與本文件線上驗證請以 Cloudflare Pages 連結為準。根目錄是民眾展示頁，完整互動 prototype 位於同站內 `/demo/`。

## 測試前提

- 主流程完全使用本機展示資料與可重播的 JavaScript 狀態。
- 小捷推薦不呼叫外部模型或商家 API；排序由本機商家資料與固定權重計算。
- 碳足跡公式以官方參考係數呈現：`小客車 104 gCO2e/km - 捷運 75 gCO2e/km = 差額 29 gCO2e/km`。
- 捷運點加成門檻固定為本月 5 kg -> `1.2x`、10 kg -> `1.5x`。
- Numbers proof 使用固定展示月份 `2026 / 05`（上月已上鏈證明）與資料指紋，讓離線 QA、截圖與現場流程可重播。
- Proof payload 只呈現月度統計與區間 bucket，不呈現完整起訖站、商家、精確時間或個人識別碼。

## ⭐ 3 分鐘完整迴圈（大會演示動線）

這條動線把「故事 → 通勤 → 孵化 → 森林 → 收成 → 兌換 → 商業模式」一次走完，約 3 分鐘，適合現場演示與評審快速驗收。

1. **開場故事（小捷世界觀）**：開啟 `/demo/` 後會自動彈出 3 頁開場故事——`小捷與碳森林的危機 (1/3)`、`將減碳化為森林養分 (2/3)`、`散步夥伴與你同行 (3/3)`。連按 `下一步` 與 `開始冒險`，進入第 4 頁「🎯 偏好設定與防護」，按 `開啟減碳冒險` 關閉。
   - 第 4 頁共 4 題：`常用通勤路線`（已預選 `永春 ↔ 忠孝復興`，可一鍵更換）、`今天想找什麼`、`通知類型`、`資料使用`。

2. **出站結算**：在 `今天` 頁 Hero 卡先看到 `☀️ 尖峰通勤`／`☕ 離峰通勤 (+1種子)` 雙切換（可先切離峰展示分流獎勵），按 `出站了，領取這趟回饋（手動模擬）`。
   - 預期按鈕變為 `已自動紀錄此趟通勤 ✓`，浮出「+N 點 / +N kg 減碳」雙數字特效，並有 🍃 綠葉飄入森林動畫（約 1.5 秒；系統開啟「減少動態效果」時自動停用）。
   - 若切在離峰，事件會記為 `完成今日離峰通勤（獎勵 +1 種子 🌱）`。
   - 完成後出現 `小捷的每日小禮物` 卡，按 `打開禮物` 可得 +10 點，並有機率加贈 1 顆稀有種子。

3. **健康任務孵化夥伴**：在 `今天` 頁「今日健康共享生活圈」點任一任務（`U-Walk 補 2,000 步`、`YouBike 接駁 10 分鐘`、`U-Sport 入場運動`）。
   - 預期對應任務蛋的孵化能量平滑充能至 100%，彈出 `🐣 能量蓄滿 · 夥伴孵化` 視窗，顯示 `【步步／輪輪／汗汗】孵化成功！` 與站點限定裝扮（例：`忠孝復興限定 · ☂️ 共享傘裝扮`），按 `迎接新夥伴` 關閉。

4. **夥伴入森林**：切到 `減碳` 頁，「你的減碳森林」卡顯示 `當前棲地`，剛孵化的夥伴會立即出現在棲地上，並列在 `🐾 夥伴在棲地探索中` 之中。

5. **立即收成**：同在 `減碳` 頁，「📅 每週日收成儀式」卡按 `立即收成`，預期無卡頓地發放 🌱 1 顆種子並解鎖本週森林卡片。
   - 選配：有種子後可按 `🌱 培育神木 (-1種子)` 灌注 +12 kg 減碳養分升級棲地，或 `📸 分享森林卡片`。

6. **首兌換券**：回 `今天` 頁頂部確認首兌進度條（點數滿 100 顯示 `🎉 已達首兌門檻！推薦兌換「全家 10 元折抵券」`）。切到 `路線` 頁，選 `全家 10 元折抵券`（用點 · 100 點），按 `使用捷運點兌換此券`。
   - 預期動畫扣點並彈出電子條碼／QR 核銷視窗，內含「資料指紋已封存 · 可稽核」與 `Provenance by Numbers Protocol` 標註；票券同步進入 `我的` 頁票券夾。

7. **B2B 營運主控台**：由左側說明面板 `📊 進入北捷／合作方營運視圖`、或 `我的` 頁 `B2B 營運合作夥伴主控台 → 進入主控台` 進入。
   - 預期看到 `台北捷運 GO · 營運主控台`：營運 Dashboard（觸及會員、推播開啟率、估算導客量、健康共享任務等）、`✓ 稽核就緒：減碳與核銷紀錄 100% 附數據指紋` 黑條、`🎯 分流與營運事件 (對映北捷 KPI)`、`🛡️ 實名導客歸因與可稽核存證`、`🌲 站點共同森林（城市大螢幕連動）` 與 `CPS LIVE 乘客端互動流`（會即時滾入剛才在乘客端完成的事件）。
   - 按 `👤 返回 C 端` 回到乘客端；乘客端各 tab 不出現任何營運指標。

## A. 線上測試流程

1. 開啟線上互動 prototype：<https://from-carbon-metro-vote.pages.dev/demo/>。
   - 預期先看到 3 頁開場故事 modal（見上方 3 分鐘迴圈第 1 步）；關閉後看到左側說明面板依序呈現 WHY（標語與一句痛點）、WHAT（價值迴圈）、HOW（3 分鐘動線與情境導演列），底部有四個快速導覽 chips：`今天`、`路線`、`減碳`、`我的`，以及 `📊 進入北捷／合作方營運視圖` 按鈕。
   - 右側手機框底部也有同樣四個 tab。
   - 預設位於 `今天`，畫面顯示「6 月 30 日 週二」、「早安，Sofia」與捷運點 `88`，頂部顯示「距首兌『全家 10 元折抵券』還差 12 點」進度條。

2. 在 `今天` 完成通勤與每日互動。
   - 按 `出站了，領取這趟回饋（手動模擬）` 後，預期按鈕文字變為 `已自動紀錄此趟通勤 ✓`。
   - 預期本月減碳由 `4.2 kg` 更新為約 `4.4 kg`，捷運點由 `88` 動畫更新為 `103`（若先答對車廂小知識則為 `105`），今日通勤顯示約 `0.23 kg` 與 `+15 點`，並跨過 100 點首兌門檻（進度條變「已達首兌門檻」）。
   - 預期畫面浮出「+15 點」與 mint 貼紙「+0.23 kg 減碳」雙數字聯動特效（約 1.3 秒），並伴隨 🍃 綠葉飄入森林動畫。
   - 選配：出站前可先在 `列車車廂陪伴` 卡答對減碳小知識（選 `75%`，+2 點）——答對後 chip 變 `🚉 到站`、車窗風景停止捲動，小捷會提示按上方按鈕出站，出站 CTA 開始脈動。
   - 選配：也可改用左側 `🎬 情境導演列` 選時段並按 `🚪 觸發出站閘門感應`，效果與手機內按鈕相同。
   - Hero 卡的 `☀️ 尖峰通勤`／`☕ 離峰通勤 (+1種子)` 切換可在完成通勤前選擇；離峰完成通勤會額外獲得 1 顆種子。
   - 預期完成通勤後出現 `小捷提醒` 卡片（例如「3 分鐘內有 2 家可用捷運點店家」），點選 `打開推薦` 會切到 `路線`；並出現 `小捷的每日小禮物` 卡片可 `打開禮物`。
   - 點選任一張「捷運點卡」後，預期顯示「捷運點 +10 / +30 / +15 已入帳」其中一種結果。
   - 預期 `小捷 · 出站小幫手` 各情境卡顯示 `既有廠商 · raingo 共享傘`、`旅電 (ChargeSPOT) 補電`、`Happ. 小樹屋電話亭`、`亞尼克 YTM 蛋糕機` 等標籤，對應真實既存廠商。
   - 預期頂部可看到首兌進度條（距首兌「全家 10 元折抵券」還差 N 點／已達首兌門檻）。
   - 點「早安，Sofia」旁的 `⚙️ 偏好設定` 可重開「偏好設定與防護」：4 題 opt-in（常用通勤路線、今天意圖、通知類型、是否同意使用進出站資料）。切換常用路線會即時改變 Hero 卡路線與 `路線` 頁副標。
   - 預期 `今日健康共享生活圈` 可看到小捷領隊的散步隊、三顆任務蛋與孵化能量；點選任務後充能至 100% 並彈出孵化視窗（見 3 分鐘迴圈第 3 步），同步加點、累積減碳、推入 B2B 端 LIVE 事件流。

3. 點選 `路線`。
   - 預期看到板南線站點 `忠孝復興`、`忠孝敦化`、`國父紀念館`、`市政府`、`永春`。
   - 預期看到 `今日路線任務`，集滿 3 枚捷運章可獲 `+50 捷運點`。
   - **已選店家詳情與花點兌換**：點擊清單中任一「用點」商家（如 `全家 10 元折抵券`、`Happ. 小樹屋電話亭` 或 `美廉社`），下方會即時顯示該推薦店家的詳細資訊與 `使用捷運點兌換此券` 按鈕。
   - 預期商家卡片會標示 `可用捷運點`、`可領捷運點`、`可換優惠券`、`共享任務`、`健康任務`、`U-Sport`、`YouBike`、`首兌特惠` 等狀態，呈現北捷場站導客與招商加值邏輯。
   - 點擊 `使用捷運點兌換此券`，若點數餘額充足，預期會動畫扣除對應捷運點，並彈出「兌換成功」電子條碼 / QR 核銷視窗。
   - 兌換成功視窗內預期顯示「資料指紋已封存 · 可稽核」區塊，含資料指紋與 `Provenance by Numbers Protocol` 標註。
   - 點選 `餐飲`、`購物`、`生活`、`共享`、`補給` 或 `健康` 篩選後，預期商家清單依分類收斂。
   - 預期路線圖站名旁顯示 raingo、ChargeSPOT、亞尼克 YTM、Happ. 小樹屋等 `站內補給站` 圖示；下方圖例卡呈現「站內就能借傘、補電、領甜點、使用電話亭」的捷運點折抵情境。
   - 點選 `在「誠品生活」集章 · +5` 或當前選取店家的集章按鈕，預期集章數增加；集滿 3 枚後顯示 `路線任務完成！`。

4. 點選 `減碳`（乘客端情感層：森林、收成與挑戰）。
   - 預期看到日 / 月 / 年切換、`本月累積減碳`、5 kg / 10 kg 門檻與 14 天趨勢圖。
   - 預期看到 `你的減碳森林`：`當前棲地`（基礎草地 → 綠野池塘 → 奇幻山丘）、已孵化夥伴在棲地探索中、`🌱 培育神木 (-1種子)` 與 `📸 分享森林卡片` 按鈕。
   - 預期看到 `📅 每週日收成儀式` 卡與 `立即收成` 按鈕（發放種子、解鎖本週森林卡片，操作不應卡頓）。
   - 預期看到 `🎯 捷運分流與減碳挑戰`（含 `☕ 離峰通勤大作戰` 等，完成後狀態變 `已完成`）。
   - 預期看到 `碳係數設定 · 可配置`，內容顯示 `小客車 104 g`、`捷運 75 g`、`差額 29 g`，可 `展開減碳公式與係數 ▾`，並註明推播時段設計參考臺北市資料大平臺「臺北捷運各站分時進出量」開放資料。
   - 點選 `日`、`月`、`年`，預期主數字與說明切換。
   - 點選 `+` 增加額外通勤，預期本月減碳與進度條同步更新；達到 10 kg 後捷運點倍率更新為 `1.5×`。
   - 點選 `−`，預期額外通勤數不會低於 `0`。
   - 注意：北捷／合作方營運 Dashboard 已獨立至 B2B 營運主控台（見下方第 7 步），乘客端 `減碳` 頁不再顯示營運指標。

5. 點選 `我的`。
   - 預期看到成就徽章牆、`我的小隊` 收集列（小捷隊長 + 步步/輪輪/汗汗 槽位）、**「我的票券 (可至實體店出示核銷)」**、`Numbers Protocol` 可信證明區塊與 opt-in 控制。
   - 可信證明區塊以「像發票存進載具一樣」比喻：減碳與核銷紀錄自動封存為可攜式數位資產，可被第三方稽核、換手機不遺失。
   - **我的票券夾**：剛才兌換的票券會展示在此列表，點擊「條碼 / QR」按鈕預期會再次彈出對應的電子條碼 / QR 核銷視窗。這可以向評審展現完整的 earn-to-spend（集點與花點）永續商業閉環。
   - opt-in 開啟時，預期看到「上月證明紀錄 · 本月結算中」，proof record 顯示 `2026 / 05`、`減碳量（去識別化）12.4 kg`、`距離區間 400–450 km`、`任務類型 每日通勤` 與資料指紋 `0x7f3a·b8e2·c91e`。
   - 點選 opt-in switch 關閉後，預期 proof record 隱藏；再點一次可恢復。
   - 點選 `重播稽核軌跡`，預期顯示 `稽核軌跡已重播：3 筆月度統計 hash 一致 ✓`。
   - Proof 畫面不應顯示完整起訖站、商家、精確時間、精確通勤距離或個人識別碼。
   - 頁面下方預期有 `B2B 營運合作夥伴主控台` 入口（`進入主控台` 按鈕）。

6. 在 `我的` 點選 `分享`。
   - 預期出現「分享減碳成果」dialog。
   - 預期 dialog 顯示本月減碳、捷運點與資料指紋。
   - 按 `Esc`、右上角 `x` 或 `完成` 可關閉。

7. 進入 B2B 營運主控台（提案商業模式視圖）。
   - 入口共兩處：左側說明面板 `📊 進入北捷／合作方營運視圖`、`我的` 頁 `進入主控台`（市民首啟流程不含營運端入口）。
   - 預期看到 `台北捷運 GO · 營運主控台` 與 `🎛️ B2B 合作夥伴營運總覽`。
   - 預期營運 Dashboard 呈現觸及會員、推播開啟率、店家頁點擊、優惠券核銷、估算導客量與健康共享任務（乘客端互動後數字會微幅跳動）。
   - 預期看到黑色 `✓ 稽核就緒：減碳與核銷紀錄 100% 附數據指紋` 橫條（Numbers Protocol）。
   - 預期看到 `🎯 分流與營運事件 (對映北捷 KPI)`：離峰通勤引導對映尖峰運能舒緩與離峰乘座率提升等營運指標。
   - 預期看到 `🛡️ 實名導客歸因與可稽核存證` ledger 與 `🌲 站點共同森林（城市大螢幕連動）` 概念看板（板南線共同神木森林）。
   - 預期底部 `CPS LIVE 乘客端互動流`（紅點 pulse）：完成通勤 / 孵化 / 收成 / 換券後事件即時滾入並附「+N 點 / +N kg」標籤；尚未互動時顯示等待提示。
   - 按 `👤 返回 C 端` 回到乘客端，確認乘客端四個 tab 完全看不到營運指標。

## B. 離線測試流程

1. 將 `offline/from-carbon-to-metro-offline.zip` 複製到沒有網路的電腦。

2. 解壓縮後進入 `from-carbon-to-metro/` 資料夾。

3. 優先用本地 server 開啟：

   ```bash
   python3 -m http.server 4173 --bind 127.0.0.1
   ```

   開啟：

   ```text
   http://127.0.0.1:4173/
   ```

   根目錄預期顯示民眾投票展示頁；完整互動 prototype 請開啟：

   ```text
   http://127.0.0.1:4173/demo/
   ```

4. 如果現場電腦沒有 Python，直接用瀏覽器開啟解壓後的 `index.html` 作為展示頁 fallback；完整互動 demo 建議仍以 static server 開啟 `demo/`。
   - 若直接開啟 `demo/index.html`，多數現代瀏覽器仍可執行 demo；如遇 script blob 或子路徑限制，改用 static server。
   - 若特定瀏覽器限制 `file://` 字型載入，畫面會回退到系統中文字型，不影響主流程判讀。

5. 依照「⭐ 3 分鐘完整迴圈」與「A. 線上測試流程」第 2 到第 7 步重跑一次。
   - 預期結果應與線上流程一致。
   - 原型不應出現等待 API、登入、付款、外部圖片、區塊鏈 RPC 或外部資料服務的阻塞狀態。

## C. 桌面與手機版檢查

1. 桌面寬度測試。
   - 使用一般筆電或桌機瀏覽器開啟。
   - 左側說明面板（WHY／WHAT／HOW）與右側手機框不應互相重疊，桌機一般視窗高度下左側面板應一屏讀完。
   - 手機框內容可捲動，底部導覽列不應遮住主要按鈕。
   - 開場故事 modal、孵化 modal、分享 dialog 開啟後不應溢出畫面。

2. 手機寬度測試。
   - 使用瀏覽器 responsive mode，建議寬度 390 px 或 420 px。
   - 四個導覽卡、手機框、底部導覽列不應水平溢出。
   - Proof record 的長 payload 可換行，不應撐破卡片。
   - 系統「減少動態效果」開啟時，綠葉飄入等動效自動停用，主流程不受影響。

## D. 素材與隱私檢查

- Repo 內保留本地字型與授權文件於 `assets/fonts/`，新版 bundle 另使用本地固定版本 runtime 於 `assets/vendor/`。
- 原型沒有使用官方捷運 Logo、未授權圖片、音樂、地圖底圖或第三方品牌素材。
- 商家名稱只作為展示情境，不含商標圖、廣告圖或交易資料。
- Proof payload 只保留月份、減碳值、距離區間、任務類型、同意狀態與資料指紋。
- 主流程沒有 secrets、tokens、API key、真實會員資料、真實票務紀錄或付款資料。

## E. 通過標準

- 線上 `/demo/` URL 可完成「⭐ 3 分鐘完整迴圈」全部 7 步：開場故事 → 出站結算 → 健康任務孵化 → 夥伴入森林 → 立即收成 → 首兌換券 → B2B 主控台。
- 完成任務後，B2B 營運主控台的 `CPS LIVE 乘客端互動流` 至少出現 1 筆對應事件。
- 兌換任一「用點」商家票券後，兌換視窗顯示「資料指紋已封存」provenance 區塊。
- 孵化 modal 顯示夥伴名稱與站點限定裝扮；`減碳` 頁森林棲地同步出現該夥伴。
- `立即收成` 操作流暢無卡頓，並成功發放種子。
- 乘客端四個 tab 不出現任何 B2B 營運指標；B2B 視圖可由兩處入口進入並可返回。
- 離線包在無網路環境可完成同一主流程。
- `我的` 頁在 opt-in 後可重現資料指紋 `0x7f3a·b8e2·c91e`。
- 外部資料服務不得阻塞主 demo。
- 桌面與手機寬度下沒有主要文字、按鈕、卡片或底部導覽列互相覆蓋。
